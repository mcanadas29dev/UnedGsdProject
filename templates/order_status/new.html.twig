{#}
{% extends 'base.html.twig' %}

{% block title %}New OrderStatus{% endblock %}

{% block body %}
    <h1>Create new OrderStatus</h1>

    {{ include('order_status/_form.html.twig') }}

    <a href="{{ path('app_order_status_index') }}">back to list</a>
{% endblock %}
#}
{% extends 'base.html.twig' %}

{% block title %}Nuevo Estado de Pedido - GreenHarvest{% endblock %}

{% block breadcrumb %}
    {% set breadcrumb = [
        {'label': 'Inicio', 'url': path('app_home')},
        {'label': 'Estados de Pedido', 'url': path('app_order_status_index')},
        {'label': 'Nuevo Estado'}
    ] %}
    {% include 'components/_breadcrumb.html.twig' with {'items': breadcrumb} %}
{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow border-0 rounded-1">
                <div class="card-body p-4">
                    <h1 class="h4 text-success fw-bold mb-4">➕ Crear nuevo Estado de Pedido</h1>
                    
                    {{ include('order_status/_form.html.twig', {'button_label': 'Guardar Estado'}) }}

                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

