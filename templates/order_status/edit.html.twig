{# {% extends 'base.html.twig' %}

{% block title %}Edit OrderStatus{% endblock %}

{% block body %}
    <h1>Edit OrderStatus</h1>

    {{ include('order_status/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_order_status_index') }}">back to list</a>

    {{ include('order_status/_delete_form.html.twig') }}
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Editar Estado de Pedido - GreenHarvest{% endblock %}

{% block breadcrumb %}
    {% set breadcrumb = [
        {'label': 'Inicio', 'url': path('app_home')},
        {'label': 'Estados de Pedido', 'url': path('app_order_status_index')},
        {'label': 'Editar Estado'}
    ] %}
    {% include 'components/_breadcrumb.html.twig' with {'items': breadcrumb} %}
{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow border-0 rounded-1">
                <div class="card-body p-4">
                    <h1 class="h4 text-success fw-bold mb-4">Editar Estado de Pedido</h1>

                    {{ include('order_status/_form.html.twig', {'button_label': 'Actualizar Estado'}) }}

  
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

