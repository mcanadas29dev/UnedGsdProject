{% extends 'base.html.twig' %}

{% block title %}Ofertas - GreenHarvest{% endblock %}

{% block body %}
<div class="container py-5">
    <!-- Encabezado -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        
        <h1 class="h3 fw-bold text-success mb-0">Listado de Ofertas</h1>
        <a href="{{ path('offer_new') }}" class="btn btn-outline-success btn-sm rounded-1 shadow-sm px-3">
            <i class="bi bi-plus-circle"></i> Añadir Oferta
        </a>
    </div>

    <!-- Botón de nueva oferta 
    <div class="d-flex justify-content-end mb-3">
        <a href="{{ path('offer_new') }}" class="btn btn-outline-success rounded-1">
            <i class="bi bi-plus-circle"></i> Nueva Oferta
        </a>
    </div> -->

    <!-- Tabla de ofertas -->
    <div class="card border-0 shadow rounded-4">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-success text-success">
                        <tr>
                            <th>ID</th>
                            <th>Producto</th>
                            <th>Precio Oferta</th>
                            <th>Inicio</th>
                            <th>Fin</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for offer in offers %}
                            <tr>
                                <td>{{ offer.id }}</td>
                                <td>{{ offer.product.name }}</td>
                                <td><span class="fw-bold text-success">{{ offer.offerPrice }} €</span></td>
                                <td>{{ offer.startDate|date('d/m/Y') }}</td>
                                <td>{{ offer.endDate|date('d/m/Y') }}</td>
                                <td class="text-center">
                                    <div class="btn-group dropup" role="group">
                                        <button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                        +
                                        </button>
                                        <ul class="dropdown-menu">
                                      
                                        <li><a class="dropdown-item text-muted" href="{{ path('offer_show', {id: offer.id}) }}" ><i class="bi bi-eye"></i> Ver</a></li>
                                        <li><a class="dropdown-item text-muted" href="{{ path('offer_edit', {id: offer.id}) }}"> <i class="bi bi-pencil-square"></i> Editar</a></li>
                                        {{ include('offer/_delete_form.html.twig', {'id': offer.id}) }}
                                        </ul>
                                    </div>
                                </td>
                                <!--
                                <td class="text-center">
                                    <div class="btn-group" role="group">
                                        <a href="{{ path('offer_show', {id: offer.id}) }}" 
                                        class="btn btn-sm btn-outline-success rounded-start" 
                                        title="Ver">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a href="{{ path('offer_edit', {id: offer.id}) }}" 
                                        class="btn btn-sm btn-outline-primary" 
                                        title="Editar">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        {{ include('offer/_delete_form.html.twig', {'id': offer.id}) }}
                                        <!--
                                        <form method="post" 
                                            action="{{ path('offer_delete', {id: offer.id}) }}" 
                                            onsubmit="return confirm('¿Seguro que quieres eliminar esta oferta?');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
                                            <button class="btn btn-sm btn-outline-danger rounded-end" title="Eliminar">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form> 
                                    </div>
                                </td> -->
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="6" class="text-center text-muted">No hay ofertas registradas.</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</div>

<div class="d-flex justify-content-center mt-4">
    {{ knp_pagination_render(offers, '@KnpPaginator/Pagination/bootstrap_v5_pagination.html.twig') }}
</div>

{% endblock %}
