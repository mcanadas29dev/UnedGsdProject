{% extends 'base.html.twig' %}

{% block title %}Códigos de respaldo 2FA | GreenHarvest{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-header bg-success text-white text-center rounded-top-4">
                    <h4 class="mb-0"><i class="bi bi-key-fill me-1"></i> Códigos de respaldo</h4>
                </div>

                <div class="card-body p-4 text-center">
                    {% if backup_codes %}
                        <p class="text-muted mb-3">
                            Guarda estos códigos en un lugar seguro.  
                            Cada uno puede usarse una sola vez si pierdes acceso a tu app 2FA.
                        </p>

                        <div class="bg-light border border-success-subtle rounded-3 p-3 text-start mx-auto" style="max-width: 300px;">
                            <ul class="list-unstyled mb-0">
                                {% for code in backup_codes %}
                                    <li class="fw-bold text-success text-monospace mb-1">{{ code }}</li>
                                {% endfor %}
                            </ul>
                        </div>

                        <p class="text-danger small mt-3 mb-0 fw-semibold">
                            ⚠️ No compartas estos códigos con nadie.
                        </p>
                    {% else %}
                        <div class="alert alert-info">
                            No hay nuevos códigos generados.
                        </div>
                    {% endif %}
                </div>

                <div class="card-footer text-center bg-light rounded-bottom-4">
                    <a href="{{ path('app_user_profile') }}" class="text-success text-decoration-none fw-semibold">
                        <i class="bi bi-arrow-left"></i> Volver al perfil
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
body {
    background: linear-gradient(135deg, #e8f5e9, #f1f8e9);
    font-family: 'Inter', sans-serif;
}
.card {
    border: 1px solid #c8e6c9;
}
</style>
{% endblock %}
