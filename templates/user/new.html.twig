{% extends 'base.html.twig' %}

{% block title %}Nuevo Usuario - GreenHarvest{% endblock %}

{% block breadcrumb %}
    {% set breadcrumb = [
        {'label': 'Inicio', 'url': path('app_home')},
        {'label': 'Usuarios', 'url': path('app_user_index')},
        {'label': 'Nuevo Usuario'}
    ] %}
    {% include 'components/_breadcrumb.html.twig' with {'items': breadcrumb} %}
{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow border-0 rounded-1">
                <div class="card-body p-4">
                    <h1 class="h4 text-success fw-bold mb-4">➕ Crear nuevo Usuario</h1>
                     {{ include('user/_form.html.twig', {'button_label': 'Nuevo Usuario'}) }}
 
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
