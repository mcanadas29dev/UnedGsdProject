<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %}Mi Tienda{% endblock %}</title>
    <!-- Bootstrap 5 CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <!-- <link rel="stylesheet" href="/css/estilos.css"> -->

</head>
<body>


{% block header %}

{% endblock %}

{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-transparent">
        <div class="container">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu" 
                aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarMenu">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_home') }}">Green Harvest</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_tienda') }}">Tienda</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_best_selling') }}">Lo Más Vendido</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="#">Últimas novedades</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="#">Ofertas</a></li>

                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-secondary" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Administración
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="adminDropdown">
                                <li><a class="dropdown-item" href="{{ path('app_user_index') }}">Usuarios</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_order_index') }}">Pedidos</a></li>
                                <li><a class="dropdown-item" href="{{ path('product_index') }}">Productos</a></li>
                                <li><a class="dropdown-item" href="{{ path('categories_list') }}">Categorías</a></li>
                                <!-- Agrega más enlaces según necesites -->
                            </ul>
                        </li>
                    {% endif %}
                </ul>
            </div>

            <div class="d-flex ms-auto">
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-primary me-3" aria-label="Cerrar sesión">
                        <i class="bi bi-box-arrow-right"></i>
                        {{ app.user.email }}  {# o email, o el campo que uses #}
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-primary me-3" aria-label="Login">
                        <i class="bi bi-box-arrow-in-right"></i>
                        Login
                    </a>
                {% endif %}

 
                <a href="{{ path('cart_index') }}" class="btn btn-outline-primary position-relative" aria-label="Carrito de compras">
                    <i class="bi bi-cart"></i>
                    {% if cart_quantity > 0 %}
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            {{ cart_quantity }}
                            <span class="visually-hidden">productos en el carrito</span>
                        </span>
                    {% endif %}
                </a>

            </div>
        </div>
    </nav>
{% endblock %}


<main class="container my-5">
    {% block body %}

    {% endblock %}
</main>

{% block footer %}
    <footer class="bg-dark text-light py-3">
        <div class="container text-center">
            <ul class="nav justify-content-center mb-3">
                <li class="nav-item"><a href="#" class="nav-link px-2 text-light">© {{ "now"|date("Y") }} Mi Tienda. Todos los derechos reservados.</a></li>
                <li class="nav-item"><a href="#" class="nav-link px-2 text-light">Privacidad</a></li>
                <li class="nav-item"><a href="#" class="nav-link px-2 text-light">Términos</a></li>
                <li class="nav-item"><a href="#" class="nav-link px-2 text-light">Ayuda</a></li>
                <li class="nav-item"><a href="#" class="nav-link px-2 text-light">Contacto</a></li>
            </ul>
         
        </div>
    </footer>
{% endblock %}


{% block javascripts %}
    <!-- Bootstrap 5 JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

{% endblock %}


</body>
</html>






