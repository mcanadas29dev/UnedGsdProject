<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %}Mi Tienda{% endblock %}</title>
    
    <!-- Bootstrap 5 CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="{{ asset('css/estilos.css') }}">
    <link rel="icon" type="image/png" sizes="16pxx16px" href="{{ asset('images/naturaleza.png') }}">
</head>
<body>
   
{% block header %}{% endblock %}

{#
{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-transparent">
        <div class="container">
            <!-- Botón para abrir sidebar 
            <button class="btn btn-outline-success me-3" id="sidebarToggle" aria-label="Abrir menú lateral">
                <i class="bi bi-list"></i>
            </button>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu" 
                aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button> -->
             <!-- Marca / Logo -->
            <a class="navbar-brand text-secondary" href="{{ path('app_home') }}">Green Harvest</a>

            <!-- 🔹 Botón hamburguesa visible en móviles -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu" 
                aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarMenu"> 
                <ul class="navbar-nav me-auto mb-2 mb-lg-0"> 
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_home') }}">Green Harvest</a></li>
                    <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-secondary" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Tienda
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="adminDropdown">
                                <li><a class="dropdown-item" href="{{ path('secciones_index') }}">Categorías</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_best_selling') }}">Lo Mas Vendido</a></li>
                                <li><a class="dropdown-item" href="{{ path('offer_index') }}">Ofertas</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_tienda') }}">Ver productos</a></li>
                               
                                <!-- Agrega más enlaces según necesites -->
                            </ul>
                    </li>
                    <!-- 
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_tienda') }}">Tienda</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('secciones_index') }}">Secciones</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_best_selling') }}">Lo Más Vendido</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('offer_index') }}">Ofertas</a></li>
                    -->
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_about') }}">Sobre Nosotros</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_news') }}">Noticias</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_contact') }}">Contacto</a></li>
                    
                    {% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') and not is_granted('ROLE_PICKER') %}
                        <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_order_user') }}">Pedidos</a></li>
                    {% endif %}

                    {% if is_granted('ROLE_PICKER') %}
                        <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_order_storage') }}">Gestión Almacén</a></li>
                    {% endif %}

                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-secondary" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Administración
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="adminDropdown">
                                <li><a class="dropdown-item" href="{{ path('app_user_index') }}">Usuarios</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_order_index') }}">Pedidos</a></li>
                                <li><a class="dropdown-item" href="{{ path('product_index') }}">Productos</a></li>
                                <li><a class="dropdown-item" href="{{ path('categories_list') }}">Categorías</a></li>
                                <li><a class="dropdown-item" href="{{ path('offer_index_admin') }}">Ofertas</a></li>
                                <!-- Agrega más enlaces según necesites -->
                            </ul>
                        </li>
                         <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('admin') }}">Easy Admin</a></li>
                    {% endif %}

                </ul>
            </div>

            <div class="d-flex ms-auto">
                {% if app.user %}
                    
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-success me-3" title={{ app.user.email }}>
                        <i class="bi bi-box-arrow-right"></i>
                        Salir {{ app.user.email }}
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-success me-3" title="Entrar">
                        <i class="bi bi-box-arrow-in-right"></i>
                        Login
                    </a>
                {% endif %}

                <a href="{{ path('cart_index') }}" class="btn btn-outline-success position-relative" aria-label="Carrito de compras">
                    <i class="bi bi-cart"></i>
                    {% if cart_quantity > 0 %}
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            {{ cart_quantity }}
                            <span class="visually-hidden">Productos en el carrito</span>
                        </span>
                    {% endif %}
                </a>
            </div>
        </div>
    </nav>
{% endblock %}
#}
{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-transparent">
        <div class="container">
            <!-- Marca / Logo -->
            {% if app.environment  == "dev" %}
                <a class="navbar-brand text-danger" href="{{ path('app_home') }}">Green Harvest PRE</a>  
            
            {% else %}
                <a class="navbar-brand text-secondary" href="{{ path('app_home') }}">Green Harvest</a>  
            {% endif %}

            <!--  Botón hamburguesa visible en móviles -->
            <button class="navbar-toggler border-success" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu"
                aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
                <i class="bi bi-list text-success fs-1"></i>
            </button>


            <!-- Menú colapsable -->
            <div class="collapse navbar-collapse" id="navbarMenu"> 
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 "> 
                    
                
                    <li class="nav-item dropdown">
                    
                        <a class="nav-link dropdown-toggle text-secondary" href="#" id="tiendaDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Tienda
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="tiendaDropdown">
                            <li><a class="dropdown-item" href="{{ path('secciones_index') }}">Categorías</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_best_selling') }}">Lo Más Vendido</a></li>
                            <li><a class="dropdown-item" href="{{ path('offer_index') }}">Ofertas</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_tienda') }}">Ver productos</a></li>
                        </ul>
                    </li>

                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_about') }}">Sobre Nosotros</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_news') }}">Noticias</a></li>
                    <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_contact') }}">Contacto</a></li>

                    {% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') and not is_granted('ROLE_PICKER') %}
                        <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_order_user') }}">Pedidos</a></li>
                    {% endif %}



                    {% if is_granted('ROLE_PICKER') %}
                        <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('app_order_storage') }}">Ops Center</a></li>
                    {% endif %}

                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-secondary" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Control Center
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="adminDropdown">
                                <li><a class="dropdown-item" href="{{ path('app_user_index') }}">Usuarios</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_order_index') }}">Ops Center</a></li>
                                <li><a class="dropdown-item" href="{{ path('product_index') }}">Productos</a></li>
                                <li><a class="dropdown-item" href="{{ path('categories_list') }}">Categorías</a></li>
                                <li><a class="dropdown-item" href="{{ path('offer_index_admin') }}">Ofertas</a></li>
                                {#<li><a class="dropdown-item" href="{{ path('offer_index_admin') }}">Marketplace</a></li> #}
                            </ul>
                        </li>
                        <!-- <li class="nav-item"><a class="nav-link text-secondary" href="{{ path('admin') }}">Easy Admin</a></li> -->
                     {% endif %}





                </ul>

                   
           

                <!-- Botones de login/logout y carrito -->
                <div class="d-flex ms-auto">
                    {# Para la funcionalidad 2FA y cambio passwd #}
                    {% if app.user %}
                        
                        <li class="nav-item dropdown list-unstyled">
                            <a class="btn btn-outline-link text-secondary" href="#" id="userDropdown" role="button" 
                            {#<a class="nav-link dropdown-toggle text-secondary" href="#" id="userDropdown" role="button" #}
                            data-bs-toggle="dropdown" aria-expanded="false"> {{ app.user.email }}               
                            </a>
                            <ul class="dropdown-menu " aria-labelledby="userDropdown">

                                <li>                                  
                                    <a class="dropdown-item text-secondary" href="{{ path('app_user_change_password') }}">
                                       <i class="bi bi-person-plus-fill">&nbsp</i> Cambiar Contraseña </a>                                   
                                </li>
                                <li>                                  
                                    <a class="dropdown-item text-secondary" href="{{ path('app_user_2fa_entry') }}">
                                       <i class="bi bi-people-fill">&nbsp</i> Activar/Desactivar 2FA </a>                                   
                                </li>
                               
                                <li>
                                    <a class="dropdown-item text-secondary" href="{{ path('app_logout') }}">                                      
                                         <i class="bi bi-box-arrow-left">&nbsp</i> Cerrar sesión  
                                    </a>
                                </li>
                              
                            </ul>
                        </li>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="btn btn-outline-link me-3 text-secondary" title="Entrar">
                            <i class="bi bi-box-arrow-in-right"></i>
                            Login
                        </a>
                    {% endif %}
                    {# Anterior login
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}" class="btn btn-outline-success me-3" title={{ app.user.email }}>
                            <i class="bi bi-box-arrow-right"></i>
                            Salir {{ app.user.email }}
                        </a>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="btn btn-outline-success me-3" title="Entrar">
                            <i class="bi bi-box-arrow-in-right"></i>
                            Login
                        </a>
                    {% endif %}
                    #}
                    <a href="{{ path('cart_index') }}" class="btn btn-outline-link text-secondary position-relative" aria-label="Carrito de compras">
                        <i class="bi bi-cart"></i>
                        {% if cart_quantity > 0 %}
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                {{ cart_quantity }}
                                <span class="visually-hidden">Productos en el carrito</span>
                            </span>
                        {% endif %}
                    </a>
                </div>
            </div>
        </div>
    </nav>
{% endblock%}

<div class="container mt-2">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb breadcrumb-modern">
            {% block breadcrumb %}{% endblock %}
        </ol>
    </nav>
</div>


<!--
<div id="sidebar" class="sidebar">
    <span class="sidebar-close" id="sidebarClose">&times;</span>
    <a href="{{ path('app_home') }}">🏠 Inicio</a>
    <a href="{{ path('app_tienda') }}">🛒 Tienda</a>
    <a href="{{ path('app_best_selling') }}">🔥 Lo Más Vendido</a>
    <a href="{{ path('app_news') }}">🆕 Últimas novedades</a>
    <a href="{{ path('offer_index') }}">💰 Ofertas</a>

    {% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') and not is_granted('ROLE_PICKER') %}
        <a href="{{ path('app_order_user') }}">📦 Pedidos</a>
    {% endif %}

    {% if is_granted('ROLE_PICKER') %}
        <a href="{{ path('app_order_storage') }}">📋 Gestión Almacén</a>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}
        <hr class="text-secondary">
        <span class="px-3 fw-bold text-light">⚙️ Administración</span>
        <a href="{{ path('app_user_index') }}">👥 Usuarios</a>
        <a href="{{ path('app_order_index') }}">📦 Pedidos</a>
        <a href="{{ path('product_index') }}">🛍️ Productos</a>
        <a href="{{ path('categories_list') }}">📂 Categorías</a>
        <a href="{{ path('offer_index_admin') }}">💡 Ofertas</a>
    {% endif %}
</div>


 Overlay 
<div id="sidebarOverlay" class="sidebar-overlay"></div>

-->
<main class="container my-5">
    <!-- Mensajes -->
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="btn-sm btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
        {% endfor %}
        </div>
    </div>
    {% block body %}

    {% endblock %}
</main>

{% block footer %}


<footer class="bg-success text-light py-3">
    <div class="container">
        <div class="row align-items-center">
            <!-- Columna 1: Copyright y versión -->
            <div class="col-md-4 text-start mb-2 mb-md-0">
                <p class="small mb-0">© {{ "now"|date("Y") }} Green Harvest - {{ app_version }}. Todos los derechos reservados.</p>
            </div>

            <!-- Columna 2: Enlaces de navegación -->
            <div class="col-md-4 text-center mb-2 mb-md-0">
                <ul class="nav justify-content-center">
                    <li class="nav-item"><a href="{{ path('app_home') }}" class="nav-link px-2 text-light">Inicio</a></li>
                    <li class="nav-item"><a href="{{ path('app_about') }}" class="nav-link px-2 text-light">Sobre Nosotros</a></li>
                    <li class="nav-item"><a href="{{ path('app_news') }}" class="nav-link px-2 text-light">Noticias</a></li>
                    <li class="nav-item"><a href="{{ path('app_contact') }}" class="nav-link px-2 text-light">Contacto</a></li>
                </ul>
            </div>

            <!-- Columna 3: Redes sociales -->
            <div class="col-md-4 text-end">
                <a href="#" class="text-light me-3 fs-5"><i class="bi bi-facebook"></i></a>
                <a href="#" class="text-light me-3 fs-5"><i class="bi bi-instagram"></i></a>
                <a href="#" class="text-light me-3 fs-5"><i class="bi bi-twitter"></i></a>
                <a href="#" class="text-light fs-5"><i class="bi bi-youtube"></i></a>
            </div>
        </div>
    </div>
</footer>

{% endblock %}

{% block javascripts %}
    <!-- Bootstrap 5 JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- <script src="{{ asset('js/scripts.js') }}"></script> -->
    <script type="module" src="{{ asset('js/scripts.js') }}"></script>

    <!--
    <script>
        const sidebar = document.getElementById('sidebar');
        const sidebarToggle = document.getElementById('sidebarToggle');
        const sidebarClose = document.getElementById('sidebarClose');
        const sidebarOverlay = document.getElementById('sidebarOverlay');

        function openSidebar() {
            sidebar.classList.add('open');
            sidebar.style.backgroundColor = "#198754"; // aquí
            sidebarOverlay.classList.add('show');
            document.body.classList.add('sidebar-open');
        }

        function closeSidebar() {
            sidebar.classList.remove('open');
            sidebarOverlay.classList.remove('show');
            document.body.classList.remove('sidebar-open');
        }

        sidebarToggle.addEventListener('click', openSidebar);
        sidebarClose.addEventListener('click', closeSidebar);
        sidebarOverlay.addEventListener('click', closeSidebar);
    </script> -->
{% endblock %}

</body>
</html>
